name: sofa-resp-sim
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - build>=1.2
      - pre-commit>=3.7
      - pytest>=8.2
      - pytest-cov>=5.0
      - ruff>=0.7
      - numpy>=1.26
      - pandas>=2.2
      - streamlit>=1.30
      - plotly>=5.24
      - ipykernel
      - jupyter
      - matplotlib
      - pyarrow
