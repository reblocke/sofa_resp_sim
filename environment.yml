name: sofa-resp-sim
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - build>=1.2
      - pre-commit>=3.7
      - pytest>=8.2
      - pytest-cov>=5.0
      - ruff>=0.7
      - pytest-playwright>=0.7
      - playwright>=1.55
      - numpy>=1.26
      - pandas>=2.2
      - ipykernel
      - jupyter
      - matplotlib
      - pyarrow
